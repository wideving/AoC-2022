import { solveDay9A } from '@/day9'
import { describe, expect, it } from 'vitest'

const PATH = 'src/day9/test-input.txt'
const FULL_PATH = 'src/day9/full-input.txt'

describe('day9 A', () => {
  it('should be defined', () => {
    expect(solveDay9A).toBeDefined()
  })
})
